{
  "name": "mova-dijital",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pip install -r backend/requirements.txt && cd frontend && npm install --legacy-peer-deps && npm run build",
    "start": "uvicorn backend.server:app --host 0.0.0.0 --port 5000"
  }
}
